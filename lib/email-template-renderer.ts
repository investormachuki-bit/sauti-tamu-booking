import { supabaseServer } from "@/lib/supabase-server";

const NAIROBI_TIME_ZONE = "Africa/Nairobi";

export type EmailTemplateRecord = {
  id: string;
  template_key: string;
  name: string;
  category: string;
  enabled: boolean;
  subject: string;
  body: string;
  variables: string[] | unknown;
  include_poster: boolean;
  poster_url: string | null;
  click_url: string | null;
};

export type EmailRenderContext = {
  full_name?: string | null;
  email?: string | null;
  whatsapp_number?: string | null;
  instrument?: string | null;
  programme?: string | null;
  course_fee?: number | string | null;
  lesson_date?: string | null;
  lesson_time?: string | null;
  payment_date?: string | null;
  amount_paid?: number | string | null;
  total_paid?: number | string | null;
  balance?: number | string | null;
  payment_method?: string | null;
  payment_reference?: string | null;
  registration_date?: string | null;
  start_date?: string | null;
  end_date?: string | null;
  booking_id?: string | null;
  student_id?: string | null;
  enrollment_id?: string | null;
  payment_id?: string | null;
  payment_history?: PaymentHistoryRow[];
  lesson_details?: {
    instrument?: string | null;
    starts_at?: string | null;
    ends_at?: string | null;
  } | null;
};

export type PaymentHistoryRow = {
  id: string;
  amount: number;
  payment_date: string;
  payment_method?: string | null;
  reference?: string | null;
};

type BusinessSettings = {
  business_name: string | null;
  phone: string | null;
  whatsapp_number: string | null;
  email: string | null;
  website: string | null;
  logo_url: string | null;
  stamp_url: string | null;
  receipt_footer: string | null;
};

function escapeHtml(value: unknown) {
  return String(value ?? "")
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;") .replace(/'/g, "&#039;"); } function normalizeVariables(value: unknown) { if (!Array.isArray(value)) return []; return value .map((item) => String(item).replace(/^{{|}}$/g, "").trim()) .filter(Boolean); } function money(value: unknown) { const number = Number(value); if (!Number.isFinite(number)) return "KSh 0"; return new Intl.NumberFormat("en-KE", { style: "currency", currency: "KES", maximumFractionDigits: 0, }).format(number); } function formatDate(value: string | null | undefined) { if (!value) return ""; const date = new Date(value); if (Number.isNaN(date.getTime())) return value; return new Intl.DateTimeFormat("en-KE", { weekday: "long", day: "numeric", month: "long", year: "numeric", timeZone: NAIROBI_TIME_ZONE, }).format(date); } function formatTime(value: string | null | undefined) { if (!value) return ""; const date = new Date(value); if (Number.isNaN(date.getTime())) return value; return new Intl.DateTimeFormat("en-KE", { hour: "numeric", minute: "2-digit", timeZone: NAIROBI_TIME_ZONE, }).format(date); } function instrumentLabel(value: string | null | undefined) { if (!value) return ""; const normalized = value.toLowerCase(); if (normalized === "piano") return "Piano"; if (normalized === "guitar") return "Acoustic Guitar"; return value; } function displayPaymentMethod(value: string | null | undefined) { if (!value) return ""; return value .replace(/_/g, " ") .replace(/\b\w/g, (letter) => letter.toUpperCase()); } function renderPaymentHistory(rows: PaymentHistoryRow[]) { if (!rows.length) { return ` <div style="margin:26px 0 0;border:1px solid #e8e8e8;border-radius:16px;overflow:hidden;background:#ffffff;"> <div style="padding:15px 18px;background:#fafafa;border-bottom:1px solid #eeeeee;"> <div style="font-size:10px;font-weight:800;letter-spacing:1.4px;color:#777777;">PAYMENT HISTORY</div> </div> <div style="padding:18px;font-size:12px;line-height:1.6;color:#666666;"> No payments have been recorded yet. </div> </div> `; } const body = rows .map( (payment) => ` <tr> <td style="padding:11px 10px;border-top:1px solid #eeeeee;font-size:11px;color:#555555;vertical-align:top;"> ${escapeHtml(formatDate(payment.payment_date))} </td> <td style="padding:11px 10px;border-top:1px solid #eeeeee;font-size:11px;color:#555555;vertical-align:top;"> ${escapeHtml(displayPaymentMethod(payment.payment_method) || "—")} </td> <td style="padding:11px 10px;border-top:1px solid #eeeeee;font-size:11px;color:#555555;vertical-align:top;"> ${escapeHtml(payment.reference || "—")} </td> <td style="padding:11px 10px;border-top:1px solid #eeeeee;font-size:11px;font-weight:800;color:#202020;text-align:right;vertical-align:top;white-space:nowrap;"> ${escapeHtml(money(payment.amount))} </td> </tr> `, ) .join(""); return ` <div style="margin:26px 0 0;border:1px solid #e8e8e8;border-radius:16px;overflow:hidden;background:#ffffff;"> <div style="padding:15px 18px;background:#fafafa;border-bottom:1px solid #eeeeee;"> <div style="font-size:10px;font-weight:800;letter-spacing:1.4px;color:#777777;">PAYMENT HISTORY</div> </div> <div style="overflow-x:auto;"> <table style="width:100%;border-collapse:collapse;min-width:520px;"> <thead> <tr> <th style="padding:10px;text-align:left;font-size:9px;letter-spacing:.4px;color:#8a8a8a;font-weight:800;">DATE</th> <th style="padding:10px;text-align:left;font-size:9px;letter-spacing:.4px;color:#8a8a8a;font-weight:800;">METHOD</th> <th style="padding:10px;text-align:left;font-size:9px;letter-spacing:.4px;color:#8a8a8a;font-weight:800;">REFERENCE</th> <th style="padding:10px;text-align:right;font-size:9px;letter-spacing:.4px;color:#8a8a8a;font-weight:800;">AMOUNT</th> </tr> </thead> <tbody>${body}</tbody> </table> </div> </div> `; } function renderLessonDetails( lesson: EmailRenderContext["lesson_details"], ) { if (!lesson) return ""; const instrument = instrumentLabel(lesson.instrument); const date = formatDate(lesson.starts_at); const time = lesson.starts_at ? `${formatTime(lesson.starts_at)}${ lesson.ends_at ? ` – ${formatTime(lesson.ends_at)}` : "" }` : ""; return ` <div style="margin:26px 0 0;border:1px solid #eadede;border-radius:18px;overflow:hidden;background:#ffffff;"> <div style="padding:13px 18px;background:#fff8f8;border-bottom:1px solid #f0dfdf;"> <div style="font-size:10px;font-weight:800;letter-spacing:1.5px;color:#b72727;">LESSON DETAILS</div> </div> <div style="padding:20px 18px 19px;"> ${ instrument ? `<div style="font-size:21px;line-height:1.2;font-weight:800;color:#1f1f1f;">${escapeHtml(instrument)}</div>` : "" } ${ date ? `<div style="margin-top:12px;font-size:13px;line-height:1.6;color:#444444;">${escapeHtml(date)}</div>` : "" } ${ time ? `<div style="margin-top:4px;font-size:20px;line-height:1.25;font-weight:800;color:#c62828;">${escapeHtml(time)}</div>` : "" } <div style="margin-top:7px;font-size:11px;color:#777777;">60-minute trial lesson</div> </div> </div> `; } function renderCourseSummary(context: EmailRenderContext) { const programme = context.programme || ""; const instrument = instrumentLabel(context.instrument); const fee = context.course_fee !== null && context.course_fee !== undefined ? money(context.course_fee) : ""; return ` <div style="margin:26px 0 0;border:1px solid #e8e8e8;border-radius:18px;overflow:hidden;background:#ffffff;"> <div style="padding:13px 18px;background:#fafafa;border-bottom:1px solid #eeeeee;"> <div style="font-size:10px;font-weight:800;letter-spacing:1.5px;color:#777777;">COURSE SUMMARY</div> </div> <div style="padding:18px;"> ${ programme ? `<div style="font-size:15px;line-height:1.45;font-weight:800;color:#202020;">${escapeHtml(programme)}</div>` : "" } ${ instrument ? `<div style="margin-top:7px;font-size:12px;color:#666666;">Instrument: ${escapeHtml(instrument)}</div>` : "" } ${ fee ? `<div style="margin-top:5px;font-size:12px;color:#666666;">Course fee: ${escapeHtml(fee)}</div>` : "" } ${ context.start_date ? `<div style="margin-top:5px;font-size:12px;color:#666666;">Start: ${escapeHtml(context.start_date)}</div>` : "" } ${ context.end_date ? `<div style="margin-top:5px;font-size:12px;color:#666666;">End: ${escapeHtml(context.end_date)}</div>` : "" } </div> </div> `; } function renderStudentBalance(context: EmailRenderContext) { const balance = Number(context.balance ?? 0); return ` <div style="margin:26px 0 0;border:1px solid #efdede;border-radius:18px;overflow:hidden;background:#fffafa;"> <div style="padding:13px 18px;border-bottom:1px solid #f3e3e3;"> <div style="font-size:10px;font-weight:800;letter-spacing:1.5px;color:#777777;">CURRENT BALANCE</div> </div> <div style="padding:18px;"> <div style="font-size:26px;line-height:1;font-weight:800;color:#c62828;"> ${escapeHtml(money(balance))} </div> </div> </div> `; } async function loadBusinessSettings(): Promise<BusinessSettings> { const { data, error } = await supabaseServer .from("business_settings") .select( "business_name, phone, whatsapp_number, email, website, logo_url, stamp_url, receipt_footer", ) .eq("id", true) .maybeSingle(); if (error) throw error; return ( (data as BusinessSettings | null) ?? { business_name: "Sauti Tamu Piano Center", phone: null, whatsapp_number: null, email: null, website: null, logo_url: null, stamp_url: null, receipt_footer: null, } ); } async function signedAssetUrl(path: string | null) { if (!path) return null; if (/^https?:\/\//i.test(path)) return path; const { data, error } = await supabaseServer.storage .from("business-assets") .createSignedUrl(path, 3600); if (error) { console.error("Email asset signing error:", error); return null; } return data?.signedUrl ?? null; } async function loadTemplate(templateKey: string) { const { data, error } = await supabaseServer .from("email_templates") .select( "id, template_key, name, category, enabled, subject, body, variables, include_poster, poster_url, click_url", ) .eq("template_key", templateKey) .maybeSingle(); if (error) throw error; return (data as EmailTemplateRecord | null) ?? null; } async function hydrateContext( context: EmailRenderContext, ): Promise<EmailRenderContext> { const hydrated = { ...context }; if (hydrated.booking_id) { const { data: booking, error: bookingError } = await supabaseServer .from("bookings") .select("id, instrument, slot_id") .eq("id", hydrated.booking_id) .maybeSingle(); if (bookingError) throw bookingError; if (booking) { hydrated.instrument = hydrated.instrument ?? booking.instrument; const { data: slot, error: slotError } = await supabaseServer .from("lesson_slots") .select("starts_at, ends_at") .eq("id", booking.slot_id) .maybeSingle(); if (slotError) throw slotError; if (slot) { hydrated.lesson_details = { instrument: hydrated.instrument, starts_at: slot.starts_at, ends_at: slot.ends_at, }; hydrated.lesson_date = hydrated.lesson_date ?? formatDate(slot.starts_at); hydrated.lesson_time = hydrated.lesson_time ?? `${formatTime(slot.starts_at)} – ${formatTime(slot.ends_at)}`; } } } if (hydrated.enrollment_id) { const { data: enrollment, error: enrollmentError } = await supabaseServer .from("student_enrollments") .select( "id, student_id, instrument, programme_name, start_date, end_date, total_fee", ) .eq("id", hydrated.enrollment_id) .maybeSingle(); if (enrollmentError) throw enrollmentError; if (enrollment) { hydrated.student_id = hydrated.student_id ?? enrollment.student_id; hydrated.instrument = hydrated.instrument ?? enrollment.instrument; hydrated.programme = hydrated.programme ?? enrollment.programme_name; hydrated.start_date = hydrated.start_date ?? enrollment.start_date; hydrated.end_date = hydrated.end_date ?? enrollment.end_date; hydrated.course_fee = hydrated.course_fee ?? enrollment.total_fee; } } if (hydrated.student_id) { const { data: student, error: studentError } = await supabaseServer .from("students") .select("full_name, email, whatsapp_number") .eq("id", hydrated.student_id) .maybeSingle(); if (studentError) throw studentError; if (student) { hydrated.full_name = hydrated.full_name ?? student.full_name; hydrated.email = hydrated.email ?? student.email; hydrated.whatsapp_number = hydrated.whatsapp_number ?? student.whatsapp_number; } } if (hydrated.enrollment_id || hydrated.student_id) { let paymentsQuery = supabaseServer .from("payments") .select( "id, student_id, enrollment_id, amount, payment_date, payment_method, reference", ) .order("payment_date", { ascending: true }) .order("created_at", { ascending: true }); if (hydrated.enrollment_id) { paymentsQuery = paymentsQuery.eq( "enrollment_id", hydrated.enrollment_id, ); } else { paymentsQuery = paymentsQuery.eq( "student_id", hydrated.student_id as string, ); } const { data: payments, error: paymentsError } = await paymentsQuery; if (paymentsError) throw paymentsError; const rows = (payments ?? []) as PaymentHistoryRow[]; hydrated.payment_history = rows; const totalPaid = rows.reduce( (sum, row) => sum + Number(row.amount || 0), 0, ); hydrated.total_paid = hydrated.total_paid ?? totalPaid; if ( hydrated.course_fee !== null && hydrated.course_fee !== undefined ) { hydrated.balance = hydrated.balance ?? Math.max(Number(hydrated.course_fee) - totalPaid, 0); } } return hydrated; } function replaceBasicVariables( text: string, context: EmailRenderContext, ) { const values: Record<string, string> = { full_name: String(context.full_name ?? ""), instrument: instrumentLabel(context.instrument), lesson_date: String(context.lesson_date ?? ""), lesson_time: String(context.lesson_time ?? ""), programme: String(context.programme ?? ""), course_fee: money(context.course_fee), payment_date: String(context.payment_date ?? ""), amount_paid: money(context.amount_paid), total_paid: money(context.total_paid), balance: money(context.balance), payment_method: displayPaymentMethod(context.payment_method), payment_reference: String(context.payment_reference ?? ""), registration_date: String(context.registration_date ?? ""), start_date: String(context.start_date ?? ""), end_date: String(context.end_date ?? ""), }; return text.replace( /{{\s*([^}]+?)\s*}}/g, (_, key: string) => { const cleanKey = key.trim(); if ( cleanKey === "payment_history" || cleanKey === "lesson_details" || cleanKey === "course_summary" || cleanKey === "student_balance" ) { return `@@SPECIAL_${cleanKey.toUpperCase()}@@`; } const value = values[cleanKey]; return value === undefined ? `{{${cleanKey}}}` : escapeHtml(value); }, ); } function applySpecialBlocks( html: string, context: EmailRenderContext, ) { return html .replace( /@@SPECIAL_PAYMENT_HISTORY@@/g, renderPaymentHistory(context.payment_history ?? []), ) .replace( /@@SPECIAL_LESSON_DETAILS@@/g, renderLessonDetails(context.lesson_details), ) .replace( /@@SPECIAL_COURSE_SUMMARY@@/g, renderCourseSummary(context), ) .replace( /@@SPECIAL_STUDENT_BALANCE@@/g, renderStudentBalance(context), ); } function renderBookingConfirmationBody( templateBody: string, context: EmailRenderContext, ) { const replaced = replaceBasicVariables(templateBody, context); const instrument = instrumentLabel(context.instrument); const lessonDate = context.lesson_date ?? ""; const lessonTime = context.lesson_time ?? ""; const businessName = "Sauti Tamu Piano Center"; return ` <div style="margin:0;padding:0;"> <div style="text-align:center;margin:0 0 8px;"> <div style="display:inline-block;padding:7px 13px;border:1px solid #efcccc;border-radius:999px;background:#fff8f8;color:#b72727;font-size:10px;line-height:1;font-weight:800;letter-spacing:1.5px;"> TRIAL LESSON CONFIRMED </div> </div> <h1 style="margin:14px 0 0;font-family:Arial,Helvetica,sans-serif;font-size:28px;line-height:1.15;letter-spacing:-.4px;text-align:center;color:#1d1d1d;"> Your Trial Lesson Is Confirmed </h1> <p style="margin:19px 0 0;font-family:Arial,Helvetica,sans-serif;font-size:15px;line-height:1.75;color:#383838;"> Hello <strong>${escapeHtml(context.full_name ?? "there")}</strong>, </p> <p style="margin:12px 0 0;font-family:Arial,Helvetica,sans-serif;font-size:15px;line-height:1.75;color:#383838;"> Your <strong>FREE ${escapeHtml(instrument)}</strong> trial lesson at <strong>${businessName}</strong> is confirmed. </p> <div style="margin:26px 0 0;border:1px solid #ebd9d9;border-radius:18px;overflow:hidden;background:#ffffff;"> <div style="padding:15px 18px;background:#fff8f8;border-bottom:1px solid #f0dfdf;"> <div style="font-family:Arial,Helvetica,sans-serif;font-size:10px;line-height:1;font-weight:800;letter-spacing:1.5px;color:#b72727;"> LESSON DETAILS </div> </div> <div style="padding:19px 18px;"> <div style="font-family:Arial,Helvetica,sans-serif;font-size:11px;line-height:1.3;font-weight:800;letter-spacing:1px;color:#8a8a8a;"> INSTRUMENT </div> <div style="margin-top:5px;font-family:Arial,Helvetica,sans-serif;font-size:19px;line-height:1.35;font-weight:800;color:#202020;"> ${escapeHtml(instrument)} </div> <div style="margin-top:16px;font-family:Arial,Helvetica,sans-serif;font-size:11px;line-height:1.3;font-weight:800;letter-spacing:1px;color:#8a8a8a;"> DATE </div> <div style="margin-top:5px;font-family:Arial,Helvetica,sans-serif;font-size:14px;line-height:1.55;color:#383838;"> ${escapeHtml(lessonDate)} </div> <div style="margin-top:16px;font-family:Arial,Helvetica,sans-serif;font-size:11px;line-height:1.3;font-weight:800;letter-spacing:1px;color:#8a8a8a;"> TIME </div> <div style="margin-top:5px;font-family:Arial,Helvetica,sans-serif;font-size:20px;line-height:1.3;font-weight:800;color:#c62828;"> ${escapeHtml(lessonTime)} </div> <div style="margin-top:16px;font-family:Arial,Helvetica,sans-serif;font-size:11px;line-height:1.3;font-weight:800;letter-spacing:1px;color:#8a8a8a;"> DURATION </div> <div style="margin-top:5px;font-family:Arial,Helvetica,sans-serif;font-size:14px;line-height:1.55;color:#383838;"> 60 minutes </div> </div> </div> <div style="margin:26px 0 0;border:1px solid #e8e8e8;border-radius:18px;overflow:hidden;background:#ffffff;"> <div style="padding:15px 18px;background:#fafafa;border-bottom:1px solid #eeeeee;"> <div style="font-family:Arial,Helvetica,sans-serif;font-size:10px;line-height:1;font-weight:800;letter-spacing:1.5px;color:#777777;"> WE ARE IN NAIROBI CBD </div> </div> <div style="padding:19px 18px;"> <div style="font-family:Arial,Helvetica,sans-serif;font-size:13px;line-height:1.8;color:#333333;"> <div style="font-weight:800;color:#202020;">📍 Location</div> <div style="margin-top:5px;"> Junction Trade Center<br /> 4th Floor, Room F401<br /> Accra Road<br /> Above Equity Bank Tearoom Branch </div> </div> </div> </div> <div style="margin:18px 0 0;border:1px solid #e8e8e8;border-radius:18px;background:#ffffff;"> <div style="padding:19px 18px;"> <div style="font-family:Arial,Helvetica,sans-serif;font-size:10px;line-height:1;font-weight:800;letter-spacing:1.5px;color:#777777;"> WORKING HOURS </div> <div style="margin-top:11px;font-family:Arial,Helvetica,sans-serif;font-size:13px;line-height:1.85;color:#333333;"> <strong>Monday – Friday:</strong> 7:00am – 7:00pm<br /> <strong>Saturday:</strong> 9:00am – 1:00pm </div> </div> </div> <div style="margin:20px 0 0;padding:15px 16px;border-left:4px solid #c62828;background:#fff8f8;border-radius:10px;"> <div style="font-family:Arial,Helvetica,sans-serif;font-size:14px;line-height:1.65;font-weight:800;color:#2a2a2a;"> Please arrive 10 minutes early. </div> </div> <div style="margin:18px 0 0;font-family:Arial,Helvetica,sans-serif;font-size:12px;line-height:1.6;color:#777777;"> We look forward to welcoming you to ${businessName}. </div> <div style="display:none;"> ${replaced ? escapeHtml(replaced).slice(0, 0) : ""} </div> </div> `; } function buildEmailShell(args: { business: BusinessSettings; logoUrl: string | null; stampUrl: string | null; posterUrl: string | null; ctaUrl: string | null; bodyHtml: string; }) { const { business, logoUrl, stampUrl, posterUrl, ctaUrl, bodyHtml, } = args; const footerLines = [ business.email, business.phone, business.website, ].filter(Boolean); const posterHtml = posterUrl ? ` <div style="margin:30px 0 0;border:1px solid #ececec;border-radius:18px;overflow:hidden;background:#f7f7f7;"> <img src="${escapeHtml(posterUrl)}" alt="Sauti Tamu Piano Center" style="display:block;width:100%;height:auto;border:0;" /> </div> ` : ""; const ctaHtml = ctaUrl ? ` <div style="margin:28px 0 0;text-align:center;"> <a href="${escapeHtml(ctaUrl)}" style="display:inline-block;background:#c62828;color:#ffffff;text-decoration:none;font-family:Arial,Helvetica,sans-serif;font-size:13px;font-weight:800;line-height:1;padding:14px 22px;border-radius:999px;" > View Details </a> </div> ` : ""; const logoHtml = logoUrl ? ` <div style="padding:24px 22px 14px;text-align:center;background:#ffffff;"> <img src="${escapeHtml(logoUrl)}" alt="${escapeHtml(business.business_name || "Sauti Tamu Piano Center")}" style="display:block;margin:0 auto;max-width:210px;width:auto;max-height:88px;height:auto;border:0;" /> </div> ` : ` <div style="padding:28px 22px 18px;text-align:center;background:#ffffff;"> <div style="font-family:Arial,Helvetica,sans-serif;font-size:24px;line-height:1;font-weight:800;letter-spacing:-.5px;color:#222222;"> ${escapeHtml(business.business_name || "Sauti Tamu Piano Center")} </div> </div> `; const stampHtml = stampUrl ? ` <div style="margin:32px 0 0;padding:24px 0 0;border-top:1px solid #e9e9e9;text-align:center;"> <img src="${escapeHtml(stampUrl)}" alt="Official stamp" style="display:block;margin:0 auto;max-width:150px;width:auto;max-height:95px;height:auto;border:0;" /> <div style="margin-top:10px;font-family:Arial,Helvetica,sans-serif;font-size:10px;line-height:1.5;color:#8a8a8a;letter-spacing:.5px;"> Official stamp </div> </div> ` : ""; const footerHtml = ` <div style="margin:26px 0 0;text-align:center;"> <div style="font-family:Arial,Helvetica,sans-serif;font-size:13px;font-weight:800;color:#333333;"> ${escapeHtml(business.business_name || "Sauti Tamu Piano Center")} </div> ${ business.receipt_footer ? `<div style="margin-top:8px;font-family:Arial,Helvetica,sans-serif;font-size:11px;line-height:1.6;color:#777777;">${escapeHtml(business.receipt_footer)}</div>` : "" } ${ footerLines.length ? `<div style="margin-top:10px;font-family:Arial,Helvetica,sans-serif;font-size:11px;line-height:1.8;color:#858585;">${footerLines .map((line) => escapeHtml(line)) .join(" · ")}</div>` : "" } </div> `; return ` <!doctype html> <html lang="en"> <head> <meta charset="utf-8" /> <meta name="viewport" content="width=device-width, initial-scale=1.0" /> <meta name="x-apple-disable-message-reformatting" /> <meta name="color-scheme" content="light" /> <meta name="supported-color-schemes" content="light" /> <title>${escapeHtml( business.business_name || "Sauti Tamu Piano Center", )}</title> </head> <body style="margin:0;padding:0;background:#efefef;"> <div style="width:100%;background:#efefef;"> <div style="max-width:680px;margin:0 auto;padding:16px 10px 30px;"> <div style="
                background:#ffffff;
                border:1px solid #e4e4e4;
                border-radius:20px;
                overflow:hidden;
                box-shadow:0 3px 18px rgba(0,0,0,.04);
              " > ${logoHtml} <div style="
                  height:4px;
                  background:#c62828;
                  line-height:4px;
                  font-size:0;
                " > &nbsp; </div> <div style="padding:26px 20px 30px;"> <div style="
                    font-family:Arial,Helvetica,sans-serif;
                    font-size:14px;
                    line-height:1.75;
                    color:#333333;
                  " > ${bodyHtml} </div> ${posterHtml} ${ctaHtml} ${stampHtml} ${footerHtml} </div> </div> </div> </div> </body> </html> `; } export async function renderSautiTamuEmail( templateKey: string, context: EmailRenderContext, ) { const [template, business] = await Promise.all([ loadTemplate(templateKey), loadBusinessSettings(), ]); if (!template) { throw new Error(`Email template not found: ${templateKey}`); } if (!template.enabled) { throw new Error(`Email template is disabled: ${templateKey}`); } const hydrated = await hydrateContext(context); if (!hydrated.full_name || !hydrated.full_name.trim()) { hydrated.full_name = "there"; } const isBookingConfirmation = templateKey === "booking_confirmation"; const bodyHtml = isBookingConfirmation ? renderBookingConfirmationBody(template.body, hydrated) : applySpecialBlocks( replaceBasicVariables(template.body, hydrated), hydrated, ); const subject = replaceBasicVariables( template.subject, hydrated, ).replace(/<[^>]+>/g, ""); const [logoUrl, stampUrl, posterUrl] = await Promise.all([ signedAssetUrl(business.logo_url), signedAssetUrl(business.stamp_url), template.include_poster ? signedAssetUrl(template.poster_url) : Promise.resolve(null), ]); const html = buildEmailShell({ business, logoUrl, stampUrl, posterUrl, ctaUrl: template.click_url, bodyHtml, }); return { subject, html, template, context: hydrated, }; }